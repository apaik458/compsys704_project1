RotaryPlant(
		input signal rotaryTableTrigger, sizeAdjust, storageAdjust;
		output signal tableAlignedWithSensor;
		)
->{
	
	while(true) {
       
        await(rotaryTableTrigger);   
        waitl(0.5 s);
        emit tableAlignedWithSensor;

        pause; // Wait until next cycle
    }
 
}
